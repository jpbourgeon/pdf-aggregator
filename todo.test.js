describe('todo', () => {
  describe('app', () => {
    it('should have an about modal dialog and a trigger button available on all pages', () => undefined);
    it('should be localized (i18n): ui and main process messages', () => undefined);
    it('should have up-to-date packages (next-6.0.3, ...)', () => undefined);
    it('should have a detailed documentation', () => undefined);
  });

  describe('index page', () => {
    it('should format and display the aggregation steps from the log', () => undefined);
    it('should provide a way to save the job\'s log to a text file when the processing is over', () => undefined);
  });

  describe('start page', () => {
    it('should have a page number option', () => undefined);
    it('should have a toc option', () => undefined);
  });
});
