describe('backlog', () => {
  describe('The app', () => {
    it('should have a detailed documentation', () => undefined);
    it('should be localized (i18n): ui and main process messages', () => undefined);
  });

  describe('Icebox', () => {
    describe('package.json', () => {
      it(
        'should use the official rkusa/pdfjs package when PR #98 will have been merged and published',
        () => undefined,
      );
      it(
        'should use the official leo/electron-next package when PR #19 will have been merged and published',
        () => undefined,
      );
    });
  });
});
